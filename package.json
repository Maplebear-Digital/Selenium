{
  "name": "selenium",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:selenium": "npx mocha --bail tests/selenium-test.js",
    "test:pdf": "npx mocha --bail tests/generate-pdf.js"
  },
  "dependencies": {
    "chromedriver": "^126.0.0",
    "mocha": "^10.7.0",
    "pdfkit": "^0.12.3",
    "selenium-webdriver": "^4.0.0"
  },
  "devDependencies": {
    "start-server-and-test": "^2.0.4"
  }
}
